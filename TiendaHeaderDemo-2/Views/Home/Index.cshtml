@{
    ViewData["Title"] = "Inicio";
}

<section class="hero">
    <h1>Header estilo tienda departamental</h1>
    <p>
        Pasa el mouse sobre una categoría para ver el panel desplegable (desktop),
        o abre el menú ☰ para probar la navegación apilable por niveles (mobile).
    </p>
</section>

<section class="demo-grid">
    <div class="demo-card">Nivel 1 → 2 → 3 renderizado desde datos mock en <code>MenuDataProvider</code>.</div>
    <div class="demo-card">Desktop: flyout tipo mega-menú por columnas al hacer hover.</div>
    <div class="demo-card">Mobile: drawer con transición tipo "push" y botón de regreso.</div>
</section>
