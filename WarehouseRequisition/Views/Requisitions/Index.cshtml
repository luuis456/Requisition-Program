@model RequisitionListViewModel
@{
    ViewData["ActiveSection"] = "pending";
}

<div class="page-heading d-flex flex-wrap align-items-center justify-content-between gap-3 mb-4">
    <div>
        <h2 class="mb-1">Requisiciones pendientes</h2>
        <p class="text-secondary mb-0">Solicitudes abiertas y en proceso esperando surtido.</p>
    </div>
    <a asp-action="Create" class="btn btn-primary rounded-pill px-4">
        <i class="bi bi-plus-lg me-1"></i> Crear requisición
    </a>
</div>

<div class="app-card mb-4">
    <div class="app-card-body">
        <form method="get" class="filter-toolbar" role="search">
            <div class="row g-2 g-md-3 align-items-end">
                <div class="col-12 col-lg-3">
                    <label class="form-label small fw-semibold">Buscar requisición...</label>
                    <div class="input-group">
                        <span class="input-group-text bg-transparent"><i class="bi bi-search"></i></span>
                        <input type="text" class="form-control" name="SearchTerm" value="@Model.Filter.SearchTerm"
                               placeholder="Número, requisitor o parte" />
                    </div>
                </div>
                <div class="col-6 col-sm-4 col-lg-2">
                    <label class="form-label small fw-semibold">Estatus</label>
                    <select class="form-select" name="Status">
                        @if (Model.StatusOptionsKey == "pending")
                        {
                            <option value="pending" selected="@(Model.Filter.Status == "pending")">Pendientes</option>
                            <option value="0" selected="@(Model.Filter.Status == "0")">Abierta</option>
                            <option value="1" selected="@(Model.Filter.Status == "1")">En proceso</option>
                            <option value="all" selected="@(Model.Filter.Status == "all")">Todas</option>
                        }
                        else
                        {
                            <option value="all-terminal" selected="@(Model.Filter.Status == "all-terminal")">Todas</option>
                            <option value="2" selected="@(Model.Filter.Status == "2")">Surtida</option>
                            <option value="3" selected="@(Model.Filter.Status == "3")">Surtida parcialmente</option>
                            <option value="4" selected="@(Model.Filter.Status == "4")">Cerrada</option>
                            <option value="5" selected="@(Model.Filter.Status == "5")">Cancelada</option>
                        }
                    </select>
                </div>
                <div class="col-6 col-sm-4 col-lg-2">
                    <label class="form-label small fw-semibold">Desde</label>
                    <input type="date" class="form-control" name="FromDate" value="@(Model.Filter.FromDate?.ToString("yyyy-MM-dd"))" />
                </div>
                <div class="col-6 col-sm-4 col-lg-2">
                    <label class="form-label small fw-semibold">Hasta</label>
                    <input type="date" class="form-control" name="ToDate" value="@(Model.Filter.ToDate?.ToString("yyyy-MM-dd"))" />
                </div>
                <div class="col-6 col-lg-2">
                    <label class="form-label small fw-semibold">Requisitor</label>
                    <input type="text" class="form-control" name="Requester" value="@Model.Filter.Requester" placeholder="Nombre..." />
                </div>
                <div class="col-6 col-sm-4 col-lg-2">
                    <label class="form-label small fw-semibold">Planta</label>
                    <select class="form-select" name="PlantId">
                        <option value="">Todas</option>
                        @foreach (var plant in Model.Plants)
                        {
                            <option value="@plant.Id" selected="@(Model.Filter.PlantId == plant.Id)">@plant.Name</option>
                        }
                    </select>
                </div>
                <div class="col-6 col-sm-4 col-lg-2">
                    <label class="form-label small fw-semibold">Área</label>
                    <select class="form-select" name="AreaId">
                        <option value="">Todas</option>
                        @foreach (var area in Model.Areas)
                        {
                            <option value="@area.Id" selected="@(Model.Filter.AreaId == area.Id)">@area.Name</option>
                        }
                    </select>
                </div>
                <div class="col-6 col-lg-2">
                    <label class="form-label small fw-semibold">Ordenar por</label>
                    <select class="form-select" name="Sort">
                        <option value="date_desc" selected="@(Model.Filter.Sort == "date_desc")">Más recientes</option>
                        <option value="date_asc" selected="@(Model.Filter.Sort == "date_asc")">Más antiguas</option>
                        <option value="number_asc" selected="@(Model.Filter.Sort == "number_asc")">Número (A-Z)</option>
                        <option value="number_desc" selected="@(Model.Filter.Sort == "number_desc")">Número (Z-A)</option>
                    </select>
                </div>
                <div class="col-6 d-flex gap-2">
                    <button type="submit" class="btn btn-primary flex-fill"><i class="bi bi-funnel me-1"></i>Filtrar</button>
                    <a asp-action="Index" class="btn btn-outline-secondary" title="Limpiar filtros"><i class="bi bi-x-lg"></i></a>
                </div>
            </div>
        </form>
    </div>
</div>

<div class="app-card">
    <div class="app-card-header">
        <div>
            <h5 class="mb-0"><i class="bi bi-list-ul me-2 text-primary"></i>@(Model.IsHistory ? "Historial de requisiciones" : "Requisiciones pendientes")</h5>
            <span class="text-secondary small">@Model.Requisitions.Count resultado(s)</span>
        </div>
    </div>
    <div class="app-card-body p-0">
        @if (Model.Requisitions.Count == 0)
        {
            <partial name="_EmptyState" model='new EmptyStateViewModel { Icon = "bi-inbox", Title = "No hay requisiciones pendientes.", Description = "Cuando Producción cree una nueva solicitud aparecerá aquí." }' />
        }
        else
        {
            <div class="table-responsive">
                <table class="table req-table align-middle mb-0">
                    <thead>
                        <tr>
                            <th>Número de requisición</th>
                            <th>Fecha de creación</th>
                            <th>Requisitor</th>
                            <th>Área</th>
                            <th>Planta</th>
                            <th>Máquina</th>
                            <th class="text-center">Materiales</th>
                            <th>Estatus</th>
                            <th class="text-end">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var requisition in Model.Requisitions)
                        {
                            var canDelete = requisition.Status is RequisitionStatus.Open or RequisitionStatus.InProgress;
                            <tr data-row="@requisition.Id">
                                <td data-label="Número">
                                    <button type="button" class="req-number fw-semibold border-0 bg-transparent p-0"
                                            data-load-details="@requisition.Id" title="Ver detalles">
                                        @requisition.RequisitionNumber
                                    </button>
                                </td>
                                <td data-label="Fecha de creación">@requisition.CreatedAt.ToString("dd/MM/yyyy HH:mm")</td>
                                <td data-label="Requisitor">
                                    <span class="fw-medium d-block">@requisition.RequesterName</span>
                                    <small class="text-secondary">Reloj @requisition.EmployeeNumber</small>
                                </td>
                                <td data-label="Área">@requisition.AreaName</td>
                                <td data-label="Planta">@requisition.PlantName</td>
                                <td data-label="Máquina">@requisition.MachineName</td>
                                <td data-label="Materiales" class="text-center"><span class="items-chip">@requisition.TotalItems</span></td>
                                <td data-label="Estatus"><partial name="_StatusBadge" model="requisition.Status" /></td>
                                <td data-label="Acciones">
                                    <div class="d-inline-flex gap-1 justify-content-end action-buttons">
                                        <button type="button" class="btn btn-icon btn-soft-primary" data-load-details="@requisition.Id"
                                                title="Ver detalles" aria-label="Ver detalles">
                                            <i class="bi bi-eye"></i>
                                        </button>
                                        <a asp-controller="Fulfillment" asp-action="Index" asp-route-requisitionNumber="@requisition.RequisitionNumber"
                                           class="btn btn-icon btn-soft-success" title="Surtir" aria-label="Surtir">
                                            <i class="bi bi-upc-scan"></i>
                                        </a>
                                        @if (canDelete)
                                        {
                                            <button type="button" class="btn btn-icon btn-soft-danger" data-delete-requisition="@requisition.Id"
                                                    data-number="@requisition.RequisitionNumber" title="Eliminar" aria-label="Eliminar">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        }
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>
</div>

<div class="modal fade details-modal" id="detailsModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-xl modal-dialog-centered modal-dialog-scrollable">
        <div class="modal-content" id="detailsModalContent"></div>
    </div>
</div>

@section Scripts {
    <script src="~/js/requisitions.js" asp-append-version="true"></script>
}
